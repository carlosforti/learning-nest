pull_request_rules:
  - name: Automatic merge when CI passes
    conditions:
      - base=main
      - status-success=node-build-test
      #- "#approved-reviews-by>=1"
    actions:
      merge:
        method: merge